apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: ai-platform
spec:
  type: LoadBalancer
  selector:
    app: api-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8000
