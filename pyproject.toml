[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-arch-dev"
version = "0.1.0"
description = "AI Architecture Development"
dependencies = []